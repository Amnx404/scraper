# Only pages whose URL starts with a base_url will be scraped
base_urls:
  - https://example.com/docs/
  - https://github.com/xyz/

output_dir: output
max_pages: 500
delay: 0.5          # seconds between requests
user_agent: python-domain-scraper/1.0
